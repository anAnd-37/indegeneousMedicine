<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/menu_profile"
        android:title="Profile"
        app:showAsAction="never" /> <!-- Set to never to prevent overcrowding on the toolbar -->
    <item
        android:id="@+id/menu_language"
        android:title="Language"
        app:showAsAction="never">
        <menu>
            <item
                android:id="@+id/menu_english"
                android:title="English" />
            <item
                android:id="@+id/menu_assamese"
                android:title="Assamese" />
        </menu>
    </item>
    <item
        android:id="@+id/menu_users"
        android:title="Users"
        app:showAsAction="never" />
</menu>
